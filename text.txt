1. Fonction : Verifie si un pipe existe (jusqu'a un pipe (|)) array
	si len d'array est 1 = une commande
encore pour >, <, >>, <<

2. Nom du fichier binaire pour le parsing
	Et apres les arguments
	string

cat -e ./txt | echo > ./txt2  :

		["cat ./txt", "echo > ./txt2"]


cat -e ./txt  :
		["cat", "-e", "./txt"]

usr/bin toutes les commandes
